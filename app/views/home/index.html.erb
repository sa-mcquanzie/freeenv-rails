<% if user_signed_in? %>
  Logged in as <strong><%= current_user.username %></strong>
  <%= current_user.tagged_commit('test-roh-repo', 'stagev2') %>
  <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
<% end %>